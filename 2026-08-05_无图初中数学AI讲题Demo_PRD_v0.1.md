# 无图初中数学 AI 讲题 Demo PRD

版本：v0.1

日期：2026-08-05

状态：待评审

面向对象：产品、教研、算法、前后端与演示人员

## 1. 文档目的

本文定义一个可以真实运行的 AI 讲题 Demo：

> 输入一道无图初中数学题、参考答案和可选的指定方法，系统生成一段由数字人讲解、语音、渐进板书和重点标注组成的完整讲题内容。

Demo 的核心任务是验证 AI 能否把“正确答案”转化为“结构清楚、理由充分、可支持迁移的教学过程”。数字人、语音合成和播放形式采用成熟服务或现成组件，不作为本阶段的核心创新点。

本文同时规定首版范围、讲题结构、板书语义、生成与校验流程、异常处理和验收标准，避免团队把主要精力消耗在数字人形象、视频格式或通用平台能力上。

---

## 2. 背景与问题

现有能力已经能够较成熟地完成：

- 输入文字后生成语音；
- 依据语音驱动数字人口型；
- 调用大模型或数学求解服务得到题目答案；
- 将文本、公式和动画渲染成网页或视频。

真正没有被解决好的问题是：

> 怎样把一道题讲清楚，使学生知道题目在问什么、为什么选择这种方法、每个关键步骤改变了什么，以及遇到同结构的新题时如何再次使用。

普通 AI 讲题容易出现以下问题：

1. 只是把参考答案换一种说法逐行念出。
2. 计算正确，但没有解释方法选择依据。
3. 每一步都展开同等长度，重点与常规运算没有区分。
4. 板书与语音重复堆叠，学生不知道当前应该看哪里。
5. 使用大量“显然”“容易得到”“我们知道”，跨过学生真正不理解的地方。
6. 给出多种方法，却没有说明它们的适用条件和差异。
7. 学生感觉听懂了，但不能独立解决同结构的新题。

因此，本 Demo 的产品对象不是“自动解题视频生成器”，而是一个受约束的“教学轨迹生成器”。

---

## 3. 产品目标

### 3.1 核心目标

验证以下端到端链路能够稳定运行：

```text
题目 + 参考答案 + 可选指定方法
→ 生成并校验正确解题图
→ 识别子目标与教学关键点
→ 生成教学轨迹
→ 生成同步语音、数字人动作与渐进板书
→ 输出可播放讲解
→ 给出同结构近迁移题
```

### 3.2 Demo 要回答的五个问题

1. AI 能否稳定把解题步骤组织成 2-4 个有意义的子目标？
2. AI 能否说明关键方法的选择依据，而不是只复述操作？
3. 数学引擎能否校验每一个关键变换、指定方法和最终答案？
4. 板书能否随着讲解渐进呈现，并始终突出当前重点？
5. 学生看完后，能否判断同结构新题应该使用什么方法？

### 3.3 Demo 不承担的结论

本阶段不声称：

- AI 讲解已经达到优秀教师水平；
- 产品能够显著提升长期成绩；
- 系统已经适配所有初中数学题型；
- 数字人能够直接提升数学迁移能力；
- 系统已经实现真正的苏格拉底式教学。

Demo 只验证技术可行性、讲题结构质量、数学正确性、视听同步和初步可理解性。正式学习效果需要后续对照实验。

---

## 4. 目标用户与使用场景

### 4.1 首版目标用户

- 初中阶段学生；
- 对当前题型不熟悉或尚未形成稳定方法的学习者；
- 默认按“初学者、高支架”生成讲解。

### 4.2 典型场景

学生已经提交一道错题或不会做的题，系统拥有题目文本和参考答案。学生点击“AI 讲解”，等待生成后观看完整讲解。

首版不要求学生在讲解过程中持续输入答案。系统可以设置“暂停思考”和“点击继续”，但不会根据学生回答进入不同分支。

### 4.3 首版题型范围

必须支持：

1. 一元一次方程，包括移项、去括号、合并同类项、分数系数。
2. 一元二次方程，包括因式分解法、公式法和配方法；输入可指定方法。

可以作为展示性扩展：

- 能够转化为一元一次方程的简短无图应用题。

明确排除：

- 含几何图形、函数图像、统计图表的题目；
- 证明题；
- 多问组合题；
- 需要动态作图或空间想象的题目；
- 参考答案明显不完整或存在争议的题目；
- 超出初中范围的复杂符号运算。

---

## 5. 方案选择

### 5.1 方案 A：大模型直接生成讲稿和板书

流程最短，开发成本最低。大模型读取题目与答案后，一次性输出讲稿、公式和时间轴。

主要问题：

- 难以保证每一步数学变换一致；
- 教学结构容易随模型和题目波动；
- 板书与讲稿可能互相矛盾；
- 出错后难以定位是求解、教学规划还是渲染问题。

不作为正式 Demo 主方案，可用于建立低成本基线。

### 5.2 方案 B：解题图与教学轨迹分离

先构造经过验证的解题图，再由 AI 从中选择教学节点、组织讲解，最后由确定性渲染器生成板书和时间轴。

优点：

- 数学正确性和教学表达可以分别校验；
- 能明确表示子目标、理由、关键点和验证步骤；
- 同一解题图可以生成不同难度、长度和风格的讲解；
- 后续容易增加互动和支架渐隐。

缺点：

- 需要定义受约束的中间结构；
- 初期只适合有限题型。

本 PRD 采用此方案。

### 5.3 方案 C：全程互动式苏格拉底 Agent

系统持续提问，根据学生回答判断误区，再动态决定下一步。

长期价值较高，但首版会同时引入语音识别、答案理解、误区诊断、动态分支和会话状态等问题，无法单独判断“讲题结构”是否成立。

不进入本 Demo，作为第二阶段方向。

---

## 6. 核心产品原则

### 6.1 教学目标优先于内容长度

每次生成前先定义：

> 学生看完后，应该能够独立完成什么？

首版默认目标是：

- 能从题目结构判断应使用的方法；
- 能说明一个关键步骤为什么成立；
- 能独立解决一道同结构、表面略有变化的近迁移题。

### 6.2 解题轨迹与教学轨迹分离

**解题轨迹**回答“怎样得到正确答案”。

**教学轨迹**回答“为了让当前学生理解，哪些步骤需要展开、解释、突出、暂停或压缩”。

参考答案只能作为解题依据，不能直接充当讲稿。

### 6.3 一个教学节点只承担一个主要认知任务

一个教学节点包括：

```text
教学节点 =
  子目标
  + 当前数学状态
  + 方法选择或操作
  + 操作理由
  + 操作后的新状态
  + 板书变化
  + 讲解语句
  + 是否需要暂停思考
```

不允许一个节点同时完成多个没有解释的数学跳跃。

### 6.4 重点由语义决定

重点不是“每一步都变色”，而是以下内容：

- 决定方法的题目结构；
- 容易选错方法的分岔点；
- 改变问题表示方式的步骤；
- 容易违反数学规则的步骤；
- 能够迁移到新题的通用原则。

常规代数计算可以压缩，关键决策不能省略。

### 6.5 数字人服务于注意力引导

数字人不持续占据主要视觉区域。它主要用于：

- 开场建立交流感；
- 提出关键问题；
- 通过视线或手势指向板书；
- 在总结阶段重新出现。

密集计算阶段允许缩小或隐藏数字人，保证板书是视觉中心。

### 6.6 唯一事实来源

系统中的事实优先级固定为：

```text
数学解释器执行后的状态
> 经过验证的 Solution Graph
> Teaching Trace
> Lesson Manifest
> 最终画面和语音
```

具体约束：

- AI 只提出下一步受限数学操作，不自行宣布操作后的真实状态。
- 数学解释器根据当前状态执行操作，生成唯一的 `state_after`。
- Teaching Trace 只能引用已验证的数学状态和语义对象 ID。
- Board Compiler 和播放器不得重新推导数学，只负责呈现。
- 参考答案是重要输入，但不是不可质疑的唯一真值；它必须与独立校验结果一致。

这个权限边界是 Demo 的核心架构决策。任何实现如果允许大模型同时自由填写 `state_before`、`operation` 和 `state_after`，都不符合本 PRD。

---

## 7. 好的讲题结构

每道题默认按照以下七个阶段组织。题目简单时允许合并相邻阶段，但不能删除方法选择、关键理由和检验。

### 7.1 阶段一：明确任务

回答：

- 题目要求得到什么？
- 最终答案应该以什么形式出现？
- 指定了哪一种方法？

输出一句简短目标，不复述全部题干。

### 7.2 阶段二：识别结构

从题目中识别决定解法的结构，例如：

- 等式两边都含未知数；
- 二次项系数为 1，适合构造完全平方；
- 左边可以分解为两个一次因式；
- 存在分母，需要先确定非零条件或去分母。

这一阶段必须说明“看见了什么”，不能直接宣布方法名称。

### 7.3 阶段三：选择方法并给出路线

说明：

- 为什么当前方法适用；
- 全题将经过哪 2-4 个子目标；
- 如果题目指定方法，说明该方法在这里如何落地。

示例：

```text
先把常数项移到右边；
再补成一个完全平方；
最后利用平方根求出两个解。
```

### 7.4 阶段四：逐个完成子目标

每个子目标遵循：

```text
当前目标 → 执行一个动作 → 说明合法性 → 展示新状态
```

常规计算允许合并，但以下情况必须解释：

- 等式两边同时进行的操作；
- 去分母、开平方、约分中的条件；
- 从一个表示切换到另一个表示；
- 产生多解、无解或增根风险的步骤。

### 7.5 阶段五：关键点暂停

全题设置 1-3 个暂停点，优先采用预测题和解释题：

- 下一步要达到什么目标？
- 两边应该同时加多少？
- 为什么这里会得到两个解？
- 这个式子具有什么结构，所以可以使用当前方法？

首版只支持“暂停—继续—揭示答案”，不根据作答分支。

### 7.6 阶段六：结果检查

根据题型选择一种最低成本的检查：

- 代回原方程；
- 检查定义域和非零条件；
- 检查答案数量是否符合当前结构；
- 检查应用题答案是否符合题意和单位。

如果代入过程过长，展示结论及关键中间值，不必完整重算。

### 7.7 阶段七：方法压缩与近迁移

最后输出三部分：

1. 一句话总结识别信号；
2. 2-4 步方法骨架；
3. 一道同结构、不同数字或表述的近迁移题。

近迁移题不在同一屏立即展示完整答案。学生点击后可查看解题骨架，完整解析不属于首版必需功能。

---

## 8. 板书定义

### 8.1 板书不是讲稿字幕

讲稿负责完整口语表达；板书只保留：

- 题目中的关键结构；
- 当前子目标；
- 数学变换；
- 必须记住的条件和理由；
- 最终方法骨架。

不得把数字人的每一句话逐字放到板书上。

### 8.2 固定语义区域

板书采用稳定布局：

| 区域 | 内容 | 生命周期 |
|---|---|---|
| 题目区 | 题目或经过压缩的关键式子 | 全程保留 |
| 路线区 | 2-4 个子目标，当前子目标高亮 | 全程保留并更新 |
| 推演区 | 当前数学步骤和必要历史状态 | 逐步增加 |
| 理由区 | 当前关键操作的理由、条件或易错点 | 随当前节点更新 |
| 总结区 | 最终答案、检查和方法骨架 | 结尾出现 |

### 8.3 首版板书动作

板书渲染器只需要支持四种核心动作：

- `write`：写入新公式或短语；
- `focus`：突出当前对象，降低其他内容视觉权重；
- `annotate`：增加箭头、圈选、括号或短标签；
- `pause`：保持画面，留出思考时间。

可选动作：

- `transform`：表达一个式子平滑变换成另一个式子；
- `clear_focus`：取消局部强调，进入下一子目标。

AI 不直接决定像素坐标、字号或动画参数。AI 只输出语义对象和动作，渲染器按固定版式排版。

### 8.4 视觉约束

- 当前步骤使用一种统一强调色。
- 理由或条件使用第二种辅助色。
- 警告和常见错误仅在确有必要时使用红色。
- 同屏最多保留 6-8 行主要推演。
- 旧步骤可以降低亮度，但关键中间状态不能消失。
- 公式出现时间应与对应语音基本同步。
- 不以模拟真人手写作为首版验收要求。

### 8.5 已确认的板书节奏

Demo 采用已经确认的组合方案：

> 传统黑板视觉 + 结构化动态引擎。

视觉上像老师在黑板上讲解，实现上像由确定状态驱动的幻灯片。每个关键数学动作按照固定节奏呈现：

```text
累积已有板书
→ 写入当前动作
→ 当前对象成为唯一焦点
→ 展示一次必要解释或语义圈画
→ 将结果沉淀为新的稳定状态
→ 进入下一动作
```

具体规则：

- 题目固定在板书顶部，不随推演消失。
- 推导从上到下累积，已完成步骤保留但降低亮度。
- 同一时刻只有一个主要焦点，默认使用暖黄色。
- 理由、条件和不变量使用独立辅助色，默认使用浅绿色。
- 圈画和箭头绑定语义 ID，例如“两边新增的 9”，不绑定字符串位置。
- 一条板书变化只对应一个主要数学动作。
- 一段关键解释只出现一次；解释结束后，画面回到简洁的数学状态。
- 板面不足时按子目标切换新板面，题目与解题路线继续保留。

### 8.6 桌面播放形态

首版默认采用横向桌面界面：

- 板书是最大的视觉区域；
- 数字人位于板书之外的独立区域，必要时可缩小或隐藏；
- 题目、推演和理由不能被数字人遮挡；
- 当前子目标持续可见；
- 公式使用结构化数学排版，不使用图片公式；
- 播放器可以按 Teaching Beat 跳转和重播。

具体比例、字体和响应式参数由设计实现确定，但不得改变“板书主、数字人辅”的注意力层级。

---

## 9. 用户流程

### 9.1 输入页

必填：

- 题目文本；
- 参考答案。

选填：

- 指定方法；
- 年级；
- 讲解长度：精简、标准；
- 数字人形象和音色。

默认值：

- 学习者水平：初学者；
- 讲解长度：标准；
- 输出形式：网页可播放 Lesson；
- 指定方法为空时，由系统选择一种主方法。

### 9.2 生成中

界面展示可理解的阶段状态：

1. 正在检查题目与答案；
2. 正在整理解题路线；
3. 正在设计讲解和板书；
4. 正在生成语音与数字人；
5. 正在合成课程。

若数学校验失败，不得继续生成看似完整的视频。

### 9.3 播放页

播放页包含：

- 数字人区域；
- 主板书区域；
- 播放、暂停、前后跳转；
- 当前子目标；
- “这一步没听懂”按钮；
- 结尾近迁移题。

“这一步没听懂”首版仅记录事件并重新播放当前教学节点，不生成新讲解。

### 9.4 生成结果

系统保存：

- 可播放 Lesson；
- 结构化 Lesson Manifest；
- 语音文件；
- 数字人视频或透明背景片段；
- 板书时间轴；
- 近迁移题；
- 校验报告。

MP4 为可选导出物，不作为系统内部的唯一格式。

### 9.5 两阶段互动形式

互动采用同一条教学时间轴分两阶段交付，不为互动重新生成另一套讲解。

**阶段一：线性讲解**

- checkpoint 已存在于 Teaching Trace；
- 播放到 checkpoint 时停顿 1-2 秒或等待学生点击继续；
- 可以先思考、再揭示答案；
- 不分析学生自由回答，不改变后续教学路径。

**阶段二：回答驱动的互动讲解**

- 播放器在 checkpoint 真正暂停并收集回答；
- 系统把回答分类为“正确、部分正确、错误或不会”；
- 不同分类改变下一 Teaching Beat；
- 学生得到帮助后必须再次尝试，不能直接跳过并视为掌握；
- 完成讲解后使用无提示近迁移题确认是否能够独立解决。

默认分支行为：

| 回答分类 | 系统动作 | 继续条件 |
|---|---|---|
| 正确 | 追问一个简短理由或让学生确认关键依据 | 理由可接受后继续 |
| 部分正确 | 指出具体卡点，提供一级提示 | 学生重新回答 |
| 错误或不会 | 局部示范最小必要步骤，再回到原问题或等价小问题 | 学生重新独立作答 |

第二阶段属于已确认的产品方向，但不属于首版 Demo 的开发验收范围。首版必须保留 checkpoint 数据和按节点恢复播放的能力，避免第二阶段重写 Lesson 模型。

---

## 10. 功能需求

### FR-01 输入规范化

系统能够：

- 识别并规范化常见数学符号；
- 保留原题文本；
- 将题目、参考答案和指定方法解析为结构化输入；
- 检查题型是否在支持范围内；
- 发现参考答案为空、明显冲突或无法解析时停止生成。

### FR-02 解题图生成

系统生成一个包含以下内容的解题图：

- 初始数学状态；
- 允许的数学操作；
- 每次操作前后的状态；
- 操作成立所需条件；
- 最终答案；
- 检验步骤；
- 与指定方法的对应关系。

一个题目可以存在多条正确路径，但首版只选择一条主路径进入教学规划。

首版允许 AI 提议的数学操作限定为：

- `simplify`
- `add_both_sides`
- `subtract_both_sides`
- `multiply_both_sides`
- `divide_both_sides`
- `expand`
- `factor`
- `combine_like_terms`
- `take_square_root_both_sides`
- `split_plus_minus`

可以在实现配方法时增加一个高层教学标签 `complete_the_square`，但底层仍应拆解为可执行、可验证的基础操作。

每一步的生成协议为：

```text
读取当前真实状态
→ AI 提出 operation 与 purpose
→ 检查 operation 是否在白名单且适用于当前状态
→ 数学解释器执行
→ 解释器生成真实新状态和语义对象 ID
→ 写入 Solution Graph
```

### FR-03 数学校验

校验器至少检查：

- 每一步等价变换或推导是否成立；
- 操作是否适用于当前状态；
- 是否符合指定方法；
- 是否遗漏定义域、非零条件或正负分支；
- 最终答案是否与参考答案一致；
- 代回原题是否成立。

校验失败时允许有限次数的结构化重试。超过次数后标记为“暂不支持”，不输出讲解。

如果使用符号计算库，不能把编程语言中的普通相等比较当作通用数学等价判断。等价性需要结合规范化、差值化简、条件检查、解集比较和代回验证；符号库不能覆盖的情况应明确拒绝。

### FR-04 教学轨迹生成

系统基于已验证的解题图生成：

- 学习目标；
- 题目结构识别；
- 方法选择依据；
- 2-4 个子目标；
- 教学节点序列；
- 1-3 个关键暂停点；
- 检验与方法总结；
- 近迁移题。

教学轨迹不能引入解题图中不存在或未经校验的数学结论。

### FR-05 教学质量检查

在进入渲染前自动检查：

- 是否明确了求解目标；
- 是否说明了方法选择依据；
- 是否存在未解释的多步跳跃；
- 讲稿与板书中的公式是否一致；
- 是否对所有常规步骤过度解释；
- 是否包含结果检查；
- 近迁移题是否与原题同结构且答案可验证。

自动检查只用于过滤明显问题，不能替代教研评价。

### FR-06 板书生成

系统把教学轨迹转换为确定性板书时间轴：

- 每个公式都有稳定 ID；
- 高亮和箭头引用已有公式或子表达式；
- 公式变化与数学状态一致；
- 超出画面容量时按子目标分页；
- 当前关键对象始终可见。

### FR-07 语音与数字人

系统能够：

- 将每个教学节点的讲稿生成语音；
- 获得每段音频时长；
- 生成或调用数字人口型视频；
- 根据音频时长调整板书动作时间；
- 在关键问题后插入可配置停顿。

首版允许数字人服务采用异步接口。某段数字人生成失败时，可以降级为静态形象加语音，不阻塞板书讲解。

### FR-08 Lesson 合成与播放

播放器读取 Lesson Manifest，按时间轴同步：

- 数字人；
- 音频；
- 板书动作；
- 当前子目标；
- 暂停点；
- 结尾迁移题。

播放器必须支持按教学节点跳转，而不只是按视频秒数拖动。

### FR-09 结果记录

至少记录：

- 各阶段生成耗时；
- 结构化重试次数；
- 校验失败原因；
- 学生播放完成率；
- 重播的教学节点；
- 是否停留并查看迁移题；
- 教研评分。

---

## 11. 核心数据对象

### 11.1 Problem Spec

```json
{
  "problem_id": "demo-001",
  "grade": 8,
  "problem_text": "用配方法解方程：x²-6x+5=0",
  "reference_answer": "x=1 或 x=5",
  "required_method": "complete_the_square",
  "learner_profile": "novice",
  "lesson_length": "standard"
}
```

### 11.2 Solution Graph

核心字段：

- `state_before`
- `operation`
- `operands`
- `purpose`
- `rule`
- `conditions`
- `state_after`
- `semantic_objects`
- `verification`
- `method_tag`

Solution Graph 只表达数学，不包含口语、颜色、坐标和数字人动作。

其中 `state_before` 来自上一个已验证状态，`state_after` 由数学解释器产生。二者不是大模型可以自由改写的文本字段。

### 11.3 Teaching Trace

核心字段：

- `learning_goal`
- `structure_cues`
- `method_rationale`
- `subgoals`
- `teaching_nodes`
- `checkpoints`
- `verification_summary`
- `method_skeleton`
- `transfer_item`

### 11.4 Lesson Manifest

核心字段：

- `scenes`
- `narration_segments`
- `audio_assets`
- `avatar_assets`
- `board_actions`
- `checkpoint_timestamps`
- `navigation_nodes`
- `validation_report`

Lesson Manifest 是播放器和可选 MP4 导出的共同输入。

### 11.5 Teaching Beat

Teaching Beat 是同步讲解运行时的最小播放单元。数字人、语音、板书和互动锚点必须由同一个 Beat 驱动。

`Lesson Director` 按顺序调度 Teaching Beat，并负责把同一个教学意图同步到数字人轨道、语音轨道和板书轨道。各轨道不能各自根据讲稿重新猜测时序。

核心字段：

- `beat_id`
- `teaching_function`
- `subgoal_id`
- `math_state_refs`
- `narration`
- `board_actions`
- `focus_targets`
- `avatar_intent`
- `audio_asset`
- `minimum_pause_ms`
- `checkpoint_id`
- `next_beat_id`

`avatar_intent` 首版只需要以下表达意图：

- `face_student`：面向学生提出目标或问题；
- `look_at_board`：把注意力引向板书；
- `explain_focus`：解释当前焦点；
- `neutral_accompany`：伴随常规推演；
- `invite_check`：回看学生并提出自检；
- `summarize`：总结方法。

音频先按 Beat 分段生成，实际音频时长决定场景基础时长；板书动作在该时长内按规则排布，AI 不直接填写精确动画秒数。

### 11.6 Checkpoint

首版即保存 Checkpoint 数据，即使播放器暂时只执行停顿和答案揭示。

核心字段：

- `checkpoint_id`
- `beat_id`
- `learning_purpose`
- `prompt`
- `response_type`
- `expected_answer`
- `acceptable_variants`
- `evaluation_rule`
- `branches`
- `hint_ladder`
- `retry_prompt`
- `resume_beat_id`
- `evidence_tag`

推荐的 checkpoint 类型：

1. 识别题目结构；
2. 选择方法；
3. 预测下一数学动作；
4. 解释关键理由；
5. 选择检查方式；
6. 无提示完成近迁移题。

首版只启用其中 1-3 个最有教学价值的 checkpoint，不能为了“看起来互动”在每一步插入问题。

---

## 12. 端到端示例

### 12.1 输入

```text
题目：用配方法解方程 x² - 6x + 5 = 0
参考答案：x = 1 或 x = 5
指定方法：配方法
```

### 12.2 解题图

```text
x² - 6x + 5 = 0
→ x² - 6x = -5
→ x² - 6x + 9 = 4
→ (x - 3)² = 4
→ x - 3 = ±2
→ x = 1 或 x = 5
```

每一条边分别标注：

- 两边同时减 5；
- 两边同时加 9；
- 完全平方公式；
- 平方根的正负分支；
- 两边同时加 3。

### 12.3 教学路线

```text
子目标 1：把常数项移到右边
子目标 2：把左边构造成完全平方
子目标 3：利用平方根求出两个解
```

### 12.4 关键教学点

关键点一：

> 为什么补 9？因为一次项系数是 -6，它的一半是 -3，平方后是 9。

关键点二：

> 从 (x-3)²=4 到下一步时，为什么必须写 ±2？

### 12.5 板书变化

1. 写出原方程，并圈出 `x² - 6x`。
2. 在路线区写出三个子目标，高亮“构造完全平方”。
3. 写出 `x² - 6x = -5`。
4. 在 `-6` 下标注“一半是 -3”，随后写出“两边同时 +9”。
5. 将左边聚焦为 `(x-3)²`。
6. 出现暂停问题：“4 的平方根只有 2 吗？”
7. 展示 `x-3=±2`，分别得到两个解。
8. 代回检查后，在总结区显示配方法骨架。

### 12.6 近迁移题

```text
用配方法解方程：x² - 8x + 7 = 0

开始前先回答：
为了构成完全平方，方程两边应该同时加多少？
```

---

## 13. 异常与降级处理

| 异常 | 系统处理 |
|---|---|
| 题型不在白名单 | 明确提示暂不支持，不调用后续合成 |
| 参考答案无法解析 | 要求更换参考答案或由教研确认 |
| 参考答案与数学引擎结果冲突 | 停止生成，输出冲突报告 |
| 指定方法不适用 | 说明方法不适用，不私自切换方法 |
| 解题图校验失败 | 最多结构化重试两次，之后终止 |
| 教学轨迹出现未验证公式 | 拒绝进入渲染并重新规划 |
| 近迁移题无法通过校验 | 重新生成迁移题，不影响主讲解 |
| TTS 失败 | 重试或切换备用音色 |
| 数字人失败 | 降级为静态形象加语音 |
| 板书溢出 | 按子目标切换新板面，保留路线区 |
| 合成中断 | 保存已完成资产，允许从失败阶段继续 |

---

## 14. 验收标准

### 14.1 验收题集

建立 30 道内部验收题：

- 一元一次方程 15 道；
- 一元二次方程 15 道；
- 覆盖简单、标准和包含易错点三个难度；
- 每种支持方法至少 5 道；
- 每道题具备教研确认的答案、主方法、子目标、关键点、常见误区和近迁移题。

其中：

- 3 道作为对外展示样例；
- 7 道作为开发调试题；
- 20 道作为不参与提示调优的保留测试集。

### 14.2 数学正确性

- 30 道题最终答案正确率为 100%。
- 所有展示出来的等价变换均通过校验。
- 指定方法遵循率为 100%。
- 任何无法校验的题不得生成“成功”结果。

### 14.3 教学结构

由至少两名初中数学教研人员独立评价：

| 维度 | 验收要求 |
|---|---|
| 题目结构识别 | 没有遗漏决定方法的核心结构 |
| 方法选择 | 清楚说明为什么使用当前方法 |
| 子目标划分 | 数量合理，具有功能意义 |
| 关键理由 | 没有跨过主要理解障碍 |
| 重点控制 | 常规步骤不过度展开，关键步骤足够突出 |
| 检查与迁移 | 包含有效检查和同结构迁移题 |

每项采用 1-5 分。20 道保留测试题中，至少 18 道的各维度平均分不低于 4 分，且不能出现数学错误。

### 14.4 视听质量

- 板书公式与讲稿内容一致；
- 当前口述公式与板书出现时间误差原则上不超过 500 毫秒；
- 不出现公式遮挡、截断或超出画面；
- 数字人不遮挡题目和主要推演区；
- 所有教学节点均可单独跳转和重播；
- 标准讲解总时长控制在 2-5 分钟。

### 14.5 技术稳定性

- 20 道保留测试题中至少 18 道一次或有限重试后成功生成；
- 单题标准讲解在正常第三方服务可用时，端到端生成时间不超过 3 分钟；
- 中间阶段失败后可以定位原因；
- 相同输入、相同版本配置下，数学轨迹和板书内容可复现。

未成功生成的题必须明确返回失败，不得以未经校验的内容降级为“成功”。因此，数学正确率按所有成功输出计算并要求为 100%，生成覆盖率按上述 18/20 单独计算。

### 14.6 初步学生验证

邀请 8-12 名目标年级学生完成小规模可用性测试。每人观看 2-3 道题。

记录：

- 是否能说出方法选择依据；
- 是否能完成一道同结构近迁移题；
- 哪个教学节点被重播；
- 学生认为最难理解的步骤；
- 板书是否帮助定位当前重点。

该测试只用于发现问题，不用于宣称正式教学效果。

---

## 15. 对照基线

为了判断复杂的教学轨迹是否真正带来价值，验收时保留一个直接生成基线：

```text
题目 + 参考答案 → 大模型生成普通逐步讲解
```

在相同题目上比较：

- 数学错误；
- 未解释跳步；
- 方法选择说明；
- 教研评分；
- 学生近迁移表现；
- 生成耗时和成本。

只有结构化方案在讲解质量上明显优于基线，后续继续建设专用中间结构才有意义。

---

## 16. 交付阶段

### 阶段一：金标准题集

交付：

- 30 道验收题；
- 每题的解题图、子目标、关键点、讲解评分标准和迁移题；
- 3 道完整展示脚本。

退出条件：教研确认题目与金标准。

### 阶段二：数学与教学轨迹

交付：

- Problem Spec；
- Solution Graph 生成与验证；
- Teaching Trace 生成；
- 自动质量检查；
- 结构化校验报告。

退出条件：20 道保留题达到数学正确性和教学结构标准。

### 阶段三：板书与语音

交付：

- 固定板书版式；
- `write/focus/annotate/pause` 动作；
- 语音生成；
- 按教学节点同步播放。

退出条件：3 道展示题通过视听验收。

### 阶段四：数字人与端到端体验

交付：

- 数字人口型服务接入；
- 生成进度页；
- 播放、暂停、重播节点和迁移题；
- 可选 MP4 导出。

退出条件：完整演示链路稳定运行。

### 阶段五：小规模验证

交付：

- 教研盲评结果；
- 学生可用性测试记录；
- 结构化方案与直接生成基线对比；
- 下一阶段 Go/No-Go 建议。

---

## 17. 风险与应对

### 17.1 AI 会生成合理但并非最佳的教学路线

应对：

- 首版限制题型；
- 建立教研金标准；
- 把“方法选择依据”和“子目标”作为显式字段；
- 允许同一解题图重新规划教学轨迹。

### 17.2 数学正确但讲解冗长

应对：

- 区分关键节点和常规节点；
- 限制子目标数和暂停点数；
- 教学质量检查识别语音与板书重复；
- 标准模式限制在 2-5 分钟。

### 17.3 参考答案本身有错

应对：

- 不把参考答案视为唯一真值；
- 数学引擎独立求解或代回验证；
- 冲突时停止生成并报告。

### 17.4 数字人分散注意力

应对：

- 密集推演时缩小或隐藏；
- 不遮挡板书；
- 通过学生注视和访谈反馈决定是否持续保留。

### 17.5 生成质量依赖单一大模型

应对：

- 中间结构独立于模型；
- 保留模型版本与提示版本；
- 核心数学操作由验证器判断；
- 允许替换模型进行横向比较。

### 17.6 Demo 被误认为已经证明教学效果

应对：

- 对外说明验证边界；
- 将教研评价、可用性测试和正式学习实验分开；
- 不用观看完成率代替学习效果。

---

## 18. Demo 成功判定

满足以下条件时，Demo 判定为成功：

1. 20 道保留测试题中至少 18 道能够稳定生成完整 Lesson。
2. 数学步骤、指定方法和最终答案全部经过验证。
3. 讲解明确呈现题目结构、方法依据、子目标、关键理由和结果检查。
4. 板书能够渐进呈现并突出当前教学重点。
5. 教研评分明显优于“直接读取参考答案生成讲稿”的基线。
6. 小规模学生测试中，至少 70% 的参与者能够说出方法选择依据，并独立开始解决近迁移题。

若只完成数字人、语音和公式动画，但无法稳定生成高质量教学轨迹，则 Demo 不视为成功。

---

## 19. Demo 之后的决策

Demo 完成后再决定：

- 是否增加应用题和更多初中数学题型；
- 是否为不同先验水平生成精简版和高支架版；
- 第二阶段互动在什么时间启动、首批启用哪些 checkpoint；
- 回答分类采用规则、模型还是两者结合；
- 是否支持多种解法的显式比较；
- 是否导出 MP4、互动网页或接入现有学习产品；
- 是否建设教师编辑和审核工具。

其中“回答会改变下一步、提示逐级增加、帮助后重新独立作答”已经确定为第二阶段互动原则。待决定的是实现时间和首批覆盖范围，不再重新讨论是否需要这种互动。

这些能力不阻塞本 Demo。首版数据模型只为已确认的互动方向保留必要接口，不提前实现完整误区诊断系统。

---

## 20. 研究依据

本 PRD 的教学设计主要参考以下研究方向：

- 数学 Worked Example Effect：正确例题对初学者具有稳定的学习增益；
- Subgoal Labeling：按功能性子目标组织步骤有助于迁移；
- Self-explanation：关键点的自我解释通常有益，但提示过密可能造成冗余；
- Example Fading：从完整例题逐步撤去支架，有助于过渡到独立解题；
- Productive Failure：短暂尝试后接受结构化讲解，更有利于概念与迁移；
- Signaling 与 Dynamic Drawing：渐进呈现和语义高亮可以引导注意；
- Instructor Presence：数字人更可能影响动机和临场感，不应被视为迁移增益的核心来源。

主要文献：

1. [A Meta-analysis of the Worked Examples Effect on Mathematics Performance](https://doi.org/10.1007/s10648-023-09745-1)
2. [Inducing Self-Explanation: A Meta-Analysis](https://doi.org/10.1007/s10648-018-9434-x)
3. [Aiding Subgoal Learning: Effects on Transfer](https://eric.ed.gov/?id=EJ505854)
4. [The Subgoal Learning Model](https://doi.org/10.1037/0096-3445.127.4.355)
5. [Transitioning From Studying Examples to Solving Problems](https://asu.elsevierpure.com/en/publications/transitioning-from-studying-examples-to-solving-problems-effects-/)
6. [When Problem Solving Followed by Instruction Works](https://journals.sagepub.com/doi/10.3102/00346543211019105)
7. [Effects of Signaling in Multimedia Learning](https://eric.ed.gov/?id=EJ1269127)
8. [Eight Ways to Promote Generative Learning](https://eric.ed.gov/?id=EJ1099178)
9. [Effects of Instructor Presence in Instructional Videos](https://www.sciencedirect.com/science/article/pii/S1747938X2300057X)

---

## 21. 新项目启动说明

### 21.1 已确认且不应重新讨论的决策

新项目默认继承以下决策：

1. 范围是无图初中数学 Demo，不建设账号、班级、题库后台或完整学习平台。
2. 核心产物是同步讲解运行时，MP4 只是可选出口。
3. 数学真实状态由解释器产生，大模型只提议受限操作和教学表达。
4. 板书采用传统黑板外观、结构化动态引擎，不模拟真实手写轨迹。
5. 板书节奏是“累积、聚焦、解释、沉淀”。
6. 数字人、语音和板书由同一个 Teaching Beat 同步驱动。
7. 首版提供线性讲解并保留 checkpoint；第二阶段在原时间轴上启用回答分支。
8. 真正的互动必须让回答改变下一步，并在帮助后要求学生重新尝试。
9. 教学质量以方法选择、关键理由和近迁移为核心，不能用格式合法或观看完成代替。

### 21.2 首个纵向切片

新项目不应同时实现全部 30 道题。第一个可运行切片固定使用：

```text
题目：用配方法解方程 x² - 6x + 5 = 0
答案：x = 1 或 x = 5
方法：配方法
学习者：初学者
```

该切片必须依次跑通：

1. 受限数学操作；
2. 解释器执行和逐步验证；
3. 三个功能性子目标；
4. Teaching Beat；
5. 累积、聚焦、解释、沉淀的板书；
6. 分段语音；
7. 数字人或静态形象降级；
8. checkpoint 停顿；
9. 近迁移题；
10. 可回放的 Lesson Package。

完成这一切片并通过人工检查后，再扩展到验收题集。

### 21.3 Lesson Package

每次成功生成输出一个自包含目录：

```text
lesson-package/
├── problem.json
├── solution_graph.json
├── teaching_trace.json
├── lesson_manifest.json
├── checkpoints.json
├── validation_report.json
├── audio/
├── avatar/
└── lesson.mp4              # 可选
```

播放器只读取 Lesson Package，不直接调用数学求解逻辑。这样网页播放器、桌面应用和 MP4 导出能够共享同一份教学状态。

### 21.4 参考实现基线

以下技术选择用于快速启动，不构成不可替换的产品依赖：

- 数学规划与验证：Python 服务，使用符号计算库辅助受限题型验证；
- 播放器与板书：TypeScript、React 和结构化数学排版组件；
- 同步：以 Teaching Beat 和真实音频时长生成时间轴；
- TTS 与数字人：通过可替换 Provider 接口接入第三方服务；
- MP4：由与播放器同构的场景组件进行可选离线渲染；
- Demo 存储：本地 Lesson Package 即可，不建设数据库和管理后台。

如果更换语言或框架，仍必须保持 Solution Graph、Teaching Trace、Teaching Beat、Checkpoint 和 Lesson Package 的边界。

### 21.5 开工前必须准备的输入

开发团队开始编码前需要获得：

- 本 PRD；
- 首个纵向切片的人工金标准；
- 3 道展示题及其教研标注；
- 首版数学操作白名单；
- Teaching Trace 与 Lesson Manifest 的 JSON Schema；
- 数学验证用例和失败用例；
- 一套 TTS 凭证或本地替代方案；
- 一套数字人接口或静态形象降级方案。

其中 JSON Schema 和验证用例应在第一个实现计划中产出，不要求在 PRD 评审前已经存在。

### 21.6 明确的未知项

以下事项尚未确认，但不会阻止第一个纵向切片开工：

- 使用哪一家大模型、TTS 和数字人供应商；
- 最终数字人形象和音色；
- 正式部署平台；
- 是否需要实时流式生成；
- 正式学生实验的样本量和研究设计；
- 第二阶段自由文本回答分类模型。

这些未知项应通过 Provider 接口或后续实验解决，不得侵入数学事实来源和教学状态模型。
